# Environment Variables for AI Image Generator

# Metrics bucket base URL (required for metrics widget)
# Example: METRICS_BUCKET_BASE=https://your-bucket.s3express-region.amazonaws.com
METRICS_BUCKET_BASE=

# DO NOT set NODE_ENV in .env.local - Next.js manages this automatically
# Setting NODE_ENV manually will cause build errors

# Optional: AWS configuration for local development
# AWS_PROFILE=default
# AWS_REGION=us-west-2
